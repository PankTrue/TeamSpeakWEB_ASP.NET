@model Tsserver
@{
    ViewData["Title"] = "Panel";
    ViewData["Header"] = $"Панель сервера {Model.Dns}.easy-ts.ru:{Model.Port}";
}

<h3>@ViewData["Header"]</h3>

<style>
    a.btn {
        width: 200px;
        height: 35px;
    }
</style>



<div class="row">
    <div class="col-md-6">

        <div class="functions">
            <ul style="list-style-type: none">
                <li> <a asp-controller="Cabinet" asp-action="Token" asp-route-id="@Model.Id" class="btn btn-primary">Токены</a></li>
                <li> <a asp-controller="Cabinet" asp-action="Settings" asp-route-id="@Model.Id" class="btn btn-primary">Настройка</a></li>
                <li> <a asp-controller="Cabinet" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Редактировать</a></li>
                <li> <a asp-controller="Cabinet" asp-action="Backups" asp-route-id="@Model.Id" class="btn btn-primary">Бекапы</a></li>
                <li> <a asp-controller="Cabinet" asp-action="TurnStatus" asp-route-id="@Model.Id" class="btn btn-primary">Вкл/Выкл</a></li>
                <li> <a asp-controller="Cabinet" asp-action="Extend" asp-route-id="@Model.Id" class="btn btn-primary">Продлить</a></li>
                <li> <a asp-controller="Cabinet" asp-action="Reset_Password" asp-route-id="@Model.Id" class="btn btn-primary">Сбросить права</a></li>
                <li> <a asp-controller="Cabinet" asp-action="Destroy" asp-route-id="@Model.Id" class="btn btn-danger" onclick="return confirm('Вы действительно хотите удалить сервер?')">Удалить</a> </li>
            </ul>
        </div>
    </div>


    <div class="col-md-6">

        <div class="tsstatus">
            @* <% panel = panel_gen.to_s.force_encoding("UTF-8") %>*@
            @*<%= @count_users %>/<%= @info['virtualserver_maxclients'] %> <br>*@
            @*<%= raw panel%>*@
        </div>

        <!--<div class="tsinfo">-->
        <!--Uptime: <%= Time.at(info['virtualserver_uptime'].to_i).strftime("%-d:%H:%M:%S") %> <br>-->
        <!-- Packet loss: <%= info['virtualserver_total_packetloss_total'] %> -->
        <!--</div>-->

    </div>


    <!-- Modal -->
    @*<div class="modal fade" id="confirm-delete" tabindex="-1" role="dialog">
        <div class="modal-dialog modal-sm">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Подтвердите действие</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <p>Удалить teamspeak сервер?</p>
                </div>
                <div class="modal-footer">
                    <button data-dismiss="modal" type="button" class="btn btn-default">Отмена</button>
                    <button id="tsserver_delete_confirm" type="button" class="btn btn-danger">Удалить</button>
                </div>
            </div>
        </div>
    </div>*@






</div>


@section Scripts {
    <script>

    </script>
}
